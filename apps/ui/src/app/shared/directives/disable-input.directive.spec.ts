import { DisableInputDirective } from './disable-input.directive';

describe('Directive: DisableInput', () => {
  it('should create an instance', () => {
    const directive = new DisableInputDirective(null);
    expect(directive).toBeTruthy();
  });
});
